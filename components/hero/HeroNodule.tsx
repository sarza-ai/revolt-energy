"use client";

/**
 * Desktop full-bleed nodule is inlined in Hero.tsx (lg+ only).
 * Mobile nodule is also inlined in Hero document flow (high, not centered).
 * This file kept for any external imports.
 */
export function HeroNodule() {
  return null;
}
