"use client";

import { InteractiveNodule } from "@/components/nodule/InteractiveNodule";

/** Hero page nodule (full-bleed interactive) */
export function HeroNodule() {
  return <InteractiveNodule variant="hero" mode="full" />;
}
